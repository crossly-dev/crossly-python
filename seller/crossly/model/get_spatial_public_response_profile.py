"""
    Crossly Seller API

    The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.  # noqa: E501

    The version of the OpenAPI document: 1.0.0
    Generated by: https://openapi-generator.tech
"""


import re  # noqa: F401
import sys  # noqa: F401

from crossly.model_utils import (  # noqa: F401
    ApiTypeError,
    ModelComposed,
    ModelNormal,
    ModelSimple,
    cached_property,
    change_keys_js_to_python,
    convert_js_args_to_python_args,
    date,
    datetime,
    file_type,
    none_type,
    validate_get_composed_info,
    OpenApiModel
)
from crossly.exceptions import ApiAttributeError


def lazy_import():
    from crossly.model.get_spatial_public_response_profile_containers import GetSpatialPublicResponseProfileContainers
    from crossly.model.get_spatial_public_response_profile_item_size import GetSpatialPublicResponseProfileItemSize
    globals()['GetSpatialPublicResponseProfileContainers'] = GetSpatialPublicResponseProfileContainers
    globals()['GetSpatialPublicResponseProfileItemSize'] = GetSpatialPublicResponseProfileItemSize


class GetSpatialPublicResponseProfile(ModelNormal):
    """NOTE: This class is auto generated by OpenAPI Generator.
    Ref: https://openapi-generator.tech

    Do not edit the class manually.

    Attributes:
      allowed_values (dict): The key is the tuple path to the attribute
          and the for var_name this is (var_name,). The value is a dict
          with a capitalized key describing the allowed value and an allowed
          value. These dicts store the allowed enum values.
      attribute_map (dict): The key is attribute name
          and the value is json key in definition.
      discriminator_value_class_map (dict): A dict to go from the discriminator
          variable value to the discriminator class name.
      validations (dict): The key is the tuple path to the attribute
          and the for var_name this is (var_name,). The value is a dict
          that stores validations for max_length, min_length, max_items,
          min_items, exclusive_maximum, inclusive_maximum, exclusive_minimum,
          inclusive_minimum, and regex.
      additional_properties_type (tuple): A tuple of classes accepted
          as additional properties values.
    """

    allowed_values = {
        ('presentation',): {
            'FLAT': "flat",
            'SLABBED': "slabbed",
            'BOXED': "boxed",
            'CASED': "cased",
            'SPINED': "spined",
            'HANGING': "hanging",
            'STANDING': "standing",
        },
        ('default_group_by',): {
            'BRAND': "brand",
            'CONDITION': "condition",
            'SIZE': "size",
            'STATUS': "status",
            'LABEL': "label",
            'TITLE': "title",
            'VALUE': "value",
            'YEAR': "year",
            'GRADE': "grade",
            'SET': "set",
            'FRANCHISE': "franchise",
            'GAME': "game",
            'TEAM': "team",
            'ERA': "era",
            'GENRE': "genre",
            'COLOUR': "colour",
            'AGE': "age",
        },
        ('default_sort_by',): {
            'BRAND': "brand",
            'CONDITION': "condition",
            'SIZE': "size",
            'STATUS': "status",
            'LABEL': "label",
            'TITLE': "title",
            'VALUE': "value",
            'YEAR': "year",
            'GRADE': "grade",
            'SET': "set",
            'FRANCHISE': "franchise",
            'GAME': "game",
            'TEAM': "team",
            'ERA': "era",
            'GENRE': "genre",
            'COLOUR': "colour",
            'AGE': "age",
        },
        ('item_model_variants',): {
            'None': None,
            'CARD': "item-card",
            'CARD-SLAB': "item-card-slab",
            'COMIC': "item-comic",
            'COMIC-SLAB': "item-comic-slab",
            'VINYL': "item-vinyl",
            'VINYL-DISC': "item-vinyl-disc",
            'FUNKO-BOX': "item-funko-box",
            'LEGO-BOX': "item-lego-box",
            'MINIFIG': "item-minifig",
            'GAME-CASE': "item-game-case",
            'BOOK': "item-book",
            'SNEAKER': "item-sneaker",
            'BOOT': "item-boot",
            'HANDBAG': "item-handbag",
            'WATCH': "item-watch",
            'PHONE': "item-phone",
            'LAPTOP': "item-laptop",
            'TEE': "item-tee",
            'HOODIE': "item-hoodie",
            'JACKET': "item-jacket",
            'JEANS': "item-jeans",
            'DRESS': "item-dress",
            'SKIRT': "item-skirt",
        },
    }

    validations = {
    }

    @cached_property
    def additional_properties_type():
        """
        This must be a method because a model may have properties that are
        of type self, this must run after the class is loaded
        """
        lazy_import()
        return (bool, date, datetime, dict, float, int, list, str, none_type,)  # noqa: E501

    _nullable = False

    @cached_property
    def openapi_types():
        """
        This must be a method because a model may have properties that are
        of type self, this must run after the class is loaded

        Returns
            openapi_types (dict): The key is attribute name
                and the value is attribute type.
        """
        lazy_import()
        return {
            'category_slug': (str,),  # noqa: E501
            'label': (str,),  # noqa: E501
            'tagline': (str,),  # noqa: E501
            'presentation': (str,),  # noqa: E501
            'item_size': (GetSpatialPublicResponseProfileItemSize,),  # noqa: E501
            'containers': ([GetSpatialPublicResponseProfileContainers],),  # noqa: E501
            'default_group_by': ([str],),  # noqa: E501
            'default_sort_by': ([str],),  # noqa: E501
            'item_model_variants': ([str], none_type,),  # noqa: E501
            'graded_variant': (str, none_type,),  # noqa: E501
        }

    @cached_property
    def discriminator():
        return None


    attribute_map = {
        'category_slug': 'categorySlug',  # noqa: E501
        'label': 'label',  # noqa: E501
        'tagline': 'tagline',  # noqa: E501
        'presentation': 'presentation',  # noqa: E501
        'item_size': 'itemSize',  # noqa: E501
        'containers': 'containers',  # noqa: E501
        'default_group_by': 'defaultGroupBy',  # noqa: E501
        'default_sort_by': 'defaultSortBy',  # noqa: E501
        'item_model_variants': 'itemModelVariants',  # noqa: E501
        'graded_variant': 'gradedVariant',  # noqa: E501
    }

    read_only_vars = {
    }

    _composed_schemas = {}

    @classmethod
    @convert_js_args_to_python_args
    def _from_openapi_data(cls, category_slug, label, tagline, presentation, item_size, containers, default_group_by, default_sort_by, *args, **kwargs):  # noqa: E501
        """GetSpatialPublicResponseProfile - a model defined in OpenAPI

        Args:
            category_slug (str): Matches `crossly_market_categories.slug`, or '*' for the fallback.
            label (str): Room title, shown in the switcher.
            tagline (str): One line of why this space is shaped the way it is. Surfaced in the UI.
            presentation (str):
            item_size (GetSpatialPublicResponseProfileItemSize):
            containers ([GetSpatialPublicResponseProfileContainers]): The container ladder, OUTERMOST FIRST. A binder holds pages, a page holds  cards. The solver walks this to decide what to create next when the  current container fills up.
            default_group_by ([str]): Default grouping, in order. Each level becomes a divider or a container.
            default_sort_by ([str]): Default ordering inside a group.

        Keyword Args:
            _check_type (bool): if True, values for parameters in openapi_types
                                will be type checked and a TypeError will be
                                raised if the wrong type is input.
                                Defaults to True
            _path_to_item (tuple/list): This is a list of keys or values to
                                drill down to the model in received_data
                                when deserializing a response
            _spec_property_naming (bool): True if the variable names in the input data
                                are serialized names, as specified in the OpenAPI document.
                                False if the variable names in the input data
                                are pythonic names, e.g. snake case (default)
            _configuration (Configuration): the instance to use when
                                deserializing a file_type parameter.
                                If passed, type conversion is attempted
                                If omitted no type conversion is done.
            _visited_composed_classes (tuple): This stores a tuple of
                                classes that we have traveled through so that
                                if we see that class again we will not use its
                                discriminator again.
                                When traveling through a discriminator, the
                                composed schema that is
                                is traveled through is added to this set.
                                For example if Animal has a discriminator
                                petType and we pass in "Dog", and the class Dog
                                allOf includes Animal, we move through Animal
                                once using the discriminator, and pick Dog.
                                Then in Dog, we will make an instance of the
                                Animal class but this time we won't travel
                                through its discriminator because we passed in
                                _visited_composed_classes = (Animal,)
            item_model_variants ([str], none_type): Other meshes items in this category may be drawn as, chosen PER ITEM.    One model per category is right for a card room, where every object is the  same object. It is wrong for a wardrobe: a rail holds tees and jeans and  jackets, and drawing all of them as a tee would be a worse lie than the  flat quad it replaced, because a wrong SHAPE reads as information.    The renderer picks from `[itemModel, ...itemModelVariants]` using the  item's title — the same heuristic `silhouetteFor` already uses to choose a  garment outline, and for the same reason: the title is the only signal  present on every item, and a wrong guess costs a slightly odd shape rather  than the wrong item. `itemModel` is the fallback when nothing matches.    Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not  an identity. See docs/IDENTIFIER-FIRST.md for where that line sits.. [optional]  # noqa: E501
            graded_variant (str, none_type): A SEPARATE profile for graded/sealed copies of the same category.    This is not a flourish. Nobody puts a slabbed card in a binder — it does  not fit and it would be vandalism. Graded cards go on a wall, raw cards go  in pockets, and a space that ignores that is immediately wrong to the only  people who would use it. Same for CGC comics.. [optional]  # noqa: E501
        """

        _check_type = kwargs.pop('_check_type', True)
        _spec_property_naming = kwargs.pop('_spec_property_naming', False)
        _path_to_item = kwargs.pop('_path_to_item', ())
        _configuration = kwargs.pop('_configuration', None)
        _visited_composed_classes = kwargs.pop('_visited_composed_classes', ())

        self = super(OpenApiModel, cls).__new__(cls)

        if args:
            raise ApiTypeError(
                "Invalid positional arguments=%s passed to %s. Remove those invalid positional arguments." % (
                    args,
                    self.__class__.__name__,
                ),
                path_to_item=_path_to_item,
                valid_classes=(self.__class__,),
            )

        self._data_store = {}
        self._check_type = _check_type
        self._spec_property_naming = _spec_property_naming
        self._path_to_item = _path_to_item
        self._configuration = _configuration
        self._visited_composed_classes = _visited_composed_classes + (self.__class__,)

        self.category_slug = category_slug
        self.label = label
        self.tagline = tagline
        self.presentation = presentation
        self.item_size = item_size
        self.containers = containers
        self.default_group_by = default_group_by
        self.default_sort_by = default_sort_by
        for var_name, var_value in kwargs.items():
            if var_name not in self.attribute_map and \
                        self._configuration is not None and \
                        self._configuration.discard_unknown_keys and \
                        self.additional_properties_type is None:
                # discard variable.
                continue
            setattr(self, var_name, var_value)
        return self

    required_properties = set([
        '_data_store',
        '_check_type',
        '_spec_property_naming',
        '_path_to_item',
        '_configuration',
        '_visited_composed_classes',
    ])

    @convert_js_args_to_python_args
    def __init__(self, category_slug, label, tagline, presentation, item_size, containers, default_group_by, default_sort_by, *args, **kwargs):  # noqa: E501
        """GetSpatialPublicResponseProfile - a model defined in OpenAPI

        Args:
            category_slug (str): Matches `crossly_market_categories.slug`, or '*' for the fallback.
            label (str): Room title, shown in the switcher.
            tagline (str): One line of why this space is shaped the way it is. Surfaced in the UI.
            presentation (str):
            item_size (GetSpatialPublicResponseProfileItemSize):
            containers ([GetSpatialPublicResponseProfileContainers]): The container ladder, OUTERMOST FIRST. A binder holds pages, a page holds  cards. The solver walks this to decide what to create next when the  current container fills up.
            default_group_by ([str]): Default grouping, in order. Each level becomes a divider or a container.
            default_sort_by ([str]): Default ordering inside a group.

        Keyword Args:
            _check_type (bool): if True, values for parameters in openapi_types
                                will be type checked and a TypeError will be
                                raised if the wrong type is input.
                                Defaults to True
            _path_to_item (tuple/list): This is a list of keys or values to
                                drill down to the model in received_data
                                when deserializing a response
            _spec_property_naming (bool): True if the variable names in the input data
                                are serialized names, as specified in the OpenAPI document.
                                False if the variable names in the input data
                                are pythonic names, e.g. snake case (default)
            _configuration (Configuration): the instance to use when
                                deserializing a file_type parameter.
                                If passed, type conversion is attempted
                                If omitted no type conversion is done.
            _visited_composed_classes (tuple): This stores a tuple of
                                classes that we have traveled through so that
                                if we see that class again we will not use its
                                discriminator again.
                                When traveling through a discriminator, the
                                composed schema that is
                                is traveled through is added to this set.
                                For example if Animal has a discriminator
                                petType and we pass in "Dog", and the class Dog
                                allOf includes Animal, we move through Animal
                                once using the discriminator, and pick Dog.
                                Then in Dog, we will make an instance of the
                                Animal class but this time we won't travel
                                through its discriminator because we passed in
                                _visited_composed_classes = (Animal,)
            item_model_variants ([str], none_type): Other meshes items in this category may be drawn as, chosen PER ITEM.    One model per category is right for a card room, where every object is the  same object. It is wrong for a wardrobe: a rail holds tees and jeans and  jackets, and drawing all of them as a tee would be a worse lie than the  flat quad it replaced, because a wrong SHAPE reads as information.    The renderer picks from `[itemModel, ...itemModelVariants]` using the  item's title — the same heuristic `silhouetteFor` already uses to choose a  garment outline, and for the same reason: the title is the only signal  present on every item, and a wrong guess costs a slightly odd shape rather  than the wrong item. `itemModel` is the fallback when nothing matches.    Deliberately NOT fuzzy-matched against a catalog — this picks a SHAPE, not  an identity. See docs/IDENTIFIER-FIRST.md for where that line sits.. [optional]  # noqa: E501
            graded_variant (str, none_type): A SEPARATE profile for graded/sealed copies of the same category.    This is not a flourish. Nobody puts a slabbed card in a binder — it does  not fit and it would be vandalism. Graded cards go on a wall, raw cards go  in pockets, and a space that ignores that is immediately wrong to the only  people who would use it. Same for CGC comics.. [optional]  # noqa: E501
        """

        _check_type = kwargs.pop('_check_type', True)
        _spec_property_naming = kwargs.pop('_spec_property_naming', False)
        _path_to_item = kwargs.pop('_path_to_item', ())
        _configuration = kwargs.pop('_configuration', None)
        _visited_composed_classes = kwargs.pop('_visited_composed_classes', ())

        if args:
            raise ApiTypeError(
                "Invalid positional arguments=%s passed to %s. Remove those invalid positional arguments." % (
                    args,
                    self.__class__.__name__,
                ),
                path_to_item=_path_to_item,
                valid_classes=(self.__class__,),
            )

        self._data_store = {}
        self._check_type = _check_type
        self._spec_property_naming = _spec_property_naming
        self._path_to_item = _path_to_item
        self._configuration = _configuration
        self._visited_composed_classes = _visited_composed_classes + (self.__class__,)

        self.category_slug = category_slug
        self.label = label
        self.tagline = tagline
        self.presentation = presentation
        self.item_size = item_size
        self.containers = containers
        self.default_group_by = default_group_by
        self.default_sort_by = default_sort_by
        for var_name, var_value in kwargs.items():
            if var_name not in self.attribute_map and \
                        self._configuration is not None and \
                        self._configuration.discard_unknown_keys and \
                        self.additional_properties_type is None:
                # discard variable.
                continue
            setattr(self, var_name, var_value)
            if var_name in self.read_only_vars:
                raise ApiAttributeError(f"`{var_name}` is a read-only attribute. Use `from_openapi_data` to instantiate "
                                     f"class with read only attributes.")
